# Binaries
/camonitor
/camonitor-*

# Local config — config.json contains plaintext camera credentials
/config.json
/config.*.json
!config.example.json

# Kubernetes secret with real camera credentials, never committed
/deploy/k8s/secret.yaml

# OS / editor cruft
.DS_Store
*.swp
*.swo
.idea/
.vscode/
